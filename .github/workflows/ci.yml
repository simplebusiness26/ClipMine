name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  web-and-docs:
    name: Web and documentation
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install JavaScript dependencies
        run: npm ci

      - name: Lint documentation
        run: npm run lint:docs

      - name: Check documentation links
        run: npm run check:links

      - name: Type-check web app
        run: npm run lint:web

      - name: Test web app
        run: npm run test:web

      - name: Build web app
        run: npm run build:web

  api:
    name: API and media pipeline
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install FFmpeg
        run: sudo apt-get update && sudo apt-get install --yes ffmpeg

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: services/api/requirements-dev.txt

      - name: Install Python dependencies
        run: python -m pip install -r services/api/requirements-dev.txt

      - name: Lint API
        run: ruff check services/api/clipmine_api services/api/tests

      - name: Test API and media pipeline
        working-directory: services/api
        run: pytest -q
